# 项目清理清单

## 📋 概述
本文档列出了项目中所有未使用和冗余的代码，按照优先级分类，便于逐步清理。

---

## 🔴 高优先级（建议立即清理）

### 1. 移除未使用的字体导入和变量
**文件：** `app/layout.tsx`

**问题：**
- 导入了 `Geist`, `Geist_Mono`, `Source_Serif_4` 字体但未使用
- 定义了 `_geist`, `_geistMono`, `_sourceSerif_4` 变量但未应用

**操作步骤：**
1. 删除第 5 行的字体导入：
   ```tsx
   // 删除这行
   import { Geist, Geist_Mono, Geist as V0_Font_Geist, Geist_Mono as V0_Font_Geist_Mono, Source_Serif_4 as V0_Font_Source_Serif_4 } from 'next/font/google'
   ```

2. 删除第 8-10 行的字体变量定义：
   ```tsx
   // 删除这些行
   const _geist = V0_Font_Geist({ subsets: ['latin'], weight: ["100","200","300","400","500","600","700","800","900"] })
   const _geistMono = V0_Font_Geist_Mono({ subsets: ['latin'], weight: ["100","200","300","400","500","600","700","800","900"] })
   const _sourceSerif_4 = V0_Font_Source_Serif_4({ subsets: ['latin'], weight: ["200","300","400","500","600","700","800","900"] })
   ```

**影响：** 减少构建体积，加快加载速度

---

### 2. 删除重复的 CSS 文件
**文件：** `styles/globals.css`

**问题：**
- `app/globals.css` 和 `styles/globals.css` 内容几乎完全相同
- `styles/globals.css` 未被任何文件引用
- `app/layout.tsx` 使用的是 `app/globals.css`

**操作步骤：**
1. 删除 `styles/globals.css` 文件

**影响：** 消除文件重复，避免混淆

---

## 🟡 中优先级（建议清理）

### 3. 删除重复的 use-toast Hook
**文件：** `components/ui/use-toast.ts`

**问题：**
- `hooks/use-toast.ts` 和 `components/ui/use-toast.ts` 内容完全相同
- 实际使用的是 `@/hooks/use-toast`
- `components/ui/use-toast.ts` 未被引用

**操作步骤：**
1. 删除 `components/ui/use-toast.ts` 文件

**影响：** 消除代码重复，减少维护成本

---

### 4. 删除重复的 use-mobile Hook
**文件：** `components/ui/use-mobile.tsx`

**问题：**
- `hooks/use-mobile.ts` 和 `components/ui/use-mobile.tsx` 内容相同
- 实际使用的是 `@/hooks/use-mobile`
- `components/ui/use-mobile.tsx` 未被引用

**操作步骤：**
1. 删除 `components/ui/use-mobile.tsx` 文件

**影响：** 消除代码重复

---

### 5. 删除未使用的 ThemeProvider 组件
**文件：** `components/theme-provider.tsx`

**问题：**
- 定义了 `ThemeProvider` 组件但从未使用
- `app/layout.tsx` 中未导入或使用

**操作步骤：**
1. 删除 `components/theme-provider.tsx` 文件

**影响：** 清理未使用的组件

---

## 🟢 低优先级（可选清理）

### 6. 删除未使用的 Toaster 组件（如果不需要 Toast 功能）
**文件：** `components/ui/toaster.tsx`

**问题：**
- 定义了 `Toaster` 组件但未在 `app/layout.tsx` 中使用
- 如果应用不需要 toast 通知功能，可以删除

**操作步骤：**
1. 删除 `components/ui/toaster.tsx` 文件
2. 如果删除，还需要删除：
   - `components/ui/toast.tsx`（如果不再需要）
   - `hooks/use-toast.ts`（如果不再需要）
   - `package.json` 中的相关依赖（需要检查是否有其他依赖）

**注意：** 如果未来可能需要 toast 功能，建议保留

---

### 7. 删除未使用的 Sidebar 组件（如果不需要 Sidebar 功能）
**文件：** `components/ui/sidebar.tsx`

**问题：**
- Sidebar 组件在应用中未被使用
- CSS 中定义了 sidebar 相关变量，但组件未使用

**操作步骤：**
1. 删除 `components/ui/sidebar.tsx` 文件
2. 可选：从 `app/globals.css` 中删除 sidebar 相关的 CSS 变量（第 32-39, 73-80, 120-127 行）

**注意：** 如果未来可能需要 sidebar 功能，建议保留

---

### 8. 清理未使用的 UI 组件（大量组件）

**问题：**
- 项目中包含 50+ 个 UI 组件，但 `app/page.tsx` 只使用了 7 个
- 这些组件可能被其他组件内部使用，需要仔细检查

**实际使用的组件：**
- ✅ `button`
- ✅ `card`
- ✅ `input`
- ✅ `label`
- ✅ `textarea`
- ✅ `slider`
- ✅ `select`

**可能未使用的组件（需要验证依赖关系）：**
- `accordion`
- `alert`
- `alert-dialog`
- `aspect-ratio`
- `avatar`
- `badge`
- `breadcrumb`
- `button-group`
- `calendar`
- `carousel`
- `chart`
- `checkbox`
- `collapsible`
- `command`
- `context-menu`
- `dialog`
- `drawer`
- `dropdown-menu`
- `empty`
- `field`
- `form`
- `hover-card`
- `input-group`
- `input-otp`
- `item`
- `kbd`
- `menubar`
- `navigation-menu`
- `pagination`
- `popover`
- `progress`
- `radio-group`
- `resizable`
- `scroll-area`
- `separator`
- `sheet`
- `skeleton`
- `sonner`
- `spinner`
- `switch`
- `table`
- `tabs`
- `toast`
- `toggle`
- `toggle-group`
- `tooltip`

**操作步骤：**
1. 使用工具检查组件依赖关系（如 `depcheck` 或手动搜索）
2. 确认哪些组件真正未被使用
3. 删除确认未使用的组件文件

**注意：** 某些组件可能被其他组件内部使用（如 `sidebar` 使用了 `sheet`, `skeleton`, `tooltip`），需要仔细检查

---

### 9. 清理未使用的依赖包

**可能未使用的依赖：**

1. **`next-themes`** (v0.4.6)
   - 用于主题切换功能
   - 但 `ThemeProvider` 组件未使用
   - **操作：** 如果不需要主题切换，可以删除

2. **`sonner`** (v1.7.4)
   - Toast 通知库
   - 但 `Toaster` 组件未在 layout 中使用
   - **操作：** 如果不需要 toast 功能，可以删除

3. **其他 Radix UI 组件依赖**
   - 许多 Radix UI 包可能只被未使用的组件依赖
   - **操作：** 删除未使用的组件后，运行 `pnpm install` 检查是否可以自动清理

**操作步骤：**
1. 删除未使用的组件后
2. 运行 `pnpm install` 更新依赖
3. 检查 `package.json` 中是否有未使用的依赖
4. 手动删除确认未使用的依赖

---

## 📊 清理统计

### 文件删除清单

**高优先级：**
- [ ] `styles/globals.css` (126 行)

**中优先级：**
- [ ] `components/ui/use-toast.ts` (192 行)
- [ ] `components/ui/use-mobile.tsx` (20 行)
- [ ] `components/theme-provider.tsx` (12 行)

**低优先级（可选）：**
- [ ] `components/ui/toaster.tsx` (36 行)
- [ ] `components/ui/sidebar.tsx` (727 行)
- [ ] 其他未使用的 UI 组件（需要验证）

### 代码修改清单

**高优先级：**
- [ ] `app/layout.tsx` - 删除字体导入和变量（约 4 行）

**低优先级（可选）：**
- [ ] `app/globals.css` - 删除 sidebar 相关 CSS 变量（如果删除 sidebar）

### 依赖清理清单

**需要验证后删除：**
- [ ] `next-themes` (如果删除 ThemeProvider)
- [ ] `sonner` (如果删除 Toaster)
- [ ] 其他 Radix UI 依赖（删除组件后检查）

---

## 🚀 执行建议

### 阶段 1：高优先级清理（立即执行）
1. 删除未使用的字体导入
2. 删除重复的 CSS 文件

### 阶段 2：中优先级清理（建议执行）
3. 删除重复的 Hook 文件
4. 删除未使用的 ThemeProvider

### 阶段 3：低优先级清理（按需执行）
5. 根据项目需求决定是否删除 Toaster、Sidebar 等组件
6. 使用工具检查并清理未使用的 UI 组件
7. 清理未使用的依赖包

---

## ⚠️ 注意事项

1. **备份：** 在执行清理前，建议先提交当前代码到 Git
2. **测试：** 每次清理后运行 `pnpm build` 和 `pnpm dev` 确保项目正常
3. **依赖检查：** 删除组件前，使用 `grep` 搜索确认没有其他文件引用
4. **渐进式清理：** 建议分阶段执行，每完成一个阶段就测试一次
5. **保留组件库：** 如果这是一个组件库项目，可能需要保留所有组件

---

## 🔍 验证命令

清理后可以使用以下命令验证：

```bash
# 检查构建是否正常
pnpm build

# 检查开发服务器是否正常
pnpm dev

# 检查未使用的依赖（需要安装 depcheck）
npx depcheck

# 搜索特定组件的使用情况
grep -r "ComponentName" app/ components/
```

---

**生成时间：** 2024
**最后更新：** 清理完成后请更新此文档
